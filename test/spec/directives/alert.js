'use strict';

describe('Directive: alert', function () {

  // load the directive's module
  beforeEach(module('angessentialsApp'));

  var scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));
});
